# keep the build context small + the image clean
.git
__pycache__
*.pyc
*.egg-info
checkpoints
data
tests
*.md
!README.md
# apps/examples/benchmarks not needed to serve the governance API —
# the image copies only apps/governance_api.py, so re-include just that one.
apps
!apps/governance_api.py
examples
benchmarks
csrc
